info:
  name: Search QuestionsByKeyword
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/questions/search"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

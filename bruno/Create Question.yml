info:
  name: Create Question
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseUrl}}/questions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

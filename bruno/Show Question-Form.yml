info:
  name: Show Question-Form
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/questions/new"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Updates the question
  type: http
  seq: 8

http:
  method: POST
  url: http://localhost:3000/questions/:id/edit
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
